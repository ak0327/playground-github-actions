name: code3.3-Override environment variables
on: push
env:
  EXAMPLE: Defined by workflow level  # 環境変数を定義
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${EXAMPLE}"
      - env:
          EXAMPLE: Defined by step level  # オーバーライド
        run: echo "${EXAMPLE}"
